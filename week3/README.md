# Week 3: Spatial Filtering

## เนื้อหาการเรียนรู้

สัปดาห์นี้เรียนรู้เกี่ยวกับ **Spatial Filtering** ซึ่งเป็นเทคนิคพื้นฐานในการประมวลผลภาพ โดยมี 2 วิธีหลัก:

---

## 📚 บทเรียน

### 1. Mean Filtering (การกรองด้วยค่าเฉลี่ย)
**ไฟล์:** [mean_filtering_chapter.ipynb](mean_filtering_chapter.ipynb)

**หลักการ:**
- แทนค่าพิกเซลแต่ละตำแหน่งด้วยค่าเฉลี่ยของพิกเซลรอบข้าง
- ช่วยลดสัญญาณรบกวน (noise) และทำให้ภาพเรียบขึ้น
- ใช้ kernel/mask ขนาด 3×3, 5×5, 7×7 เป็นต้น

**ขั้นตอนการทำงาน:**
1. กำหนดบริเวณรอบข้าง (Neighborhood)
2. คำนวณค่าเฉลี่ยของพิกเซลในหน้าต่าง
3. แทนค่าพิกเซลเดิมด้วยค่าเฉลี่ยที่คำนวณได้

**วิธีการที่เรียน:**
- ✅ **cv2.blur()** - ใช้ค่าเฉลี่ยธรรมดา (เร็วที่สุด)
- ✅ **cv2.filter2D()** - ใช้ Gaussian kernel (คุณภาพดี)
- ✅ **Custom Implementation** - สร้างฟังก์ชันเอง (เพื่อเข้าใจวิธีการ)

**Kernel Types:**
- **Box Filter:** ทุกพิกเซลมีน้ำหนักเท่ากัน (1/9)
- **Gaussian Filter:** พิกเซลกลางมีน้ำหนักมากกว่า (4/16)

---

### 2. Median Filtering (การกรองด้วยค่ามัธยฐาน)
**ไฟล์:** [median_filtering_chapter.ipynb](median_filtering_chapter.ipynb)

**หลักการ:**
- แทนค่าพิกเซลแต่ละตำแหน่งด้วยค่ามัธยฐานของพิกเซลรอบข้าง
- มีประสิทธิภาพเป็นพิเศษในการลบ **salt and pepper noise**
- รักษาขอบของภาพได้ดีกว่า mean filtering

**ขั้นตอนการทำงาน:**
1. กำหนดบริเวณรอบข้าง (เช่น 3×3)
2. เรียงลำดับค่าพิกเซลทั้งหมดในหน้าต่าง
3. เลือกค่าที่อยู่ตรงกลาง (median) มาแทนพิกเซลเดิม

**วิธีการที่เรียน:**
- ✅ **cv2.medianBlur()** - ใช้ library OpenCV
- ✅ **Custom Implementation** - สร้างฟังก์ชันเอง โดยใช้ `np.sort()` และ `np.ravel()`

---

## 🔍 เปรียบเทียบ Mean vs Median Filtering

| คุณสมบัติ | Mean Filtering | Median Filtering |
|-----------|----------------|------------------|
| **วิธีการ** | ค่าเฉลี่ย | ค่ามัธยฐาน |
| **เหมาะกับ Noise** | Gaussian noise | Salt & Pepper noise |
| **การรักษาขอบ** | เบลอขอบ | รักษาขอบได้ดี |
| **ความเร็ว** | เร็วกว่า | ช้ากว่า |
| **ผลลัพธ์** | เรียบ/นุ่ม | คมชัดกว่า |

---

## 📁 ไฟล์ในโฟลเดอร์

```
week3/
├── mean_filtering_chapter.ipynb      # บทเรียน Mean Filtering
├── median_filtering_chapter.ipynb    # บทเรียน Median Filtering
├── README.md                          # ไฟล์นี้
└── img/                               # โฟลเดอร์รูปภาพ
    └── lena_salt_512.png             # ภาพตัวอย่างสำหรับทดลอง
```

---

## 💡 สิ่งที่ควรจำ

1. **Mean Filtering** เหมาะกับการลด noise ทั่วไป แต่อาจทำให้ภาพเบลอ
2. **Median Filtering** เหมาะกับ salt & pepper noise และรักษาขอบได้ดี
3. ขนาด kernel ยิ่งใหญ่ → ยิ่งเบลอมาก
4. OpenCV มีฟังก์ชันสำเร็จรูปที่เร็วและมีประสิทธิภาพ
5. การสร้างฟังก์ชันเองช่วยให้เข้าใจหลักการทำงานลึกซึ้งขึ้น

---

## 🚀 การใช้งาน

```python
import cv2
import numpy as np

# อ่านภาพ
img = cv2.imread('./img/lena_salt_512.png', 0)

# Mean Filtering
mean_result = cv2.blur(img, (3, 3))

# Median Filtering
median_result = cv2.medianBlur(img, 3)

# แสดงผลลัพธ์
cv2.imshow('Original', img)
cv2.imshow('Mean', mean_result)
cv2.imshow('Median', median_result)
cv2.waitKey(0)
```

---

## 📖 References

- Digital Image Processing (Gonzalez & Woods)
- OpenCV Documentation
- NumPy Documentation
